# Infrastructure and secrets only. Everything configurable per project lives in the DB
# (projects.config) and is edited from the web UI — never here.
SMITH_DATABASE_URL=postgresql+psycopg://smith:smith@localhost:5433/reviewer
SMITH_SECRET_KEY=change-me-openssl-rand-hex-32
SMITH_COOKIE_SECURE=false
