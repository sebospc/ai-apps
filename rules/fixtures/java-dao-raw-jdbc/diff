diff --git a/core/src/com/acme/core/dao/impl/DefaultOrderReportDao.java b/core/src/com/acme/core/dao/impl/DefaultOrderReportDao.java
--- a/core/src/com/acme/core/dao/impl/DefaultOrderReportDao.java
+++ b/core/src/com/acme/core/dao/impl/DefaultOrderReportDao.java
@@ -17,6 +17,21 @@
    private DataSource dataSource;
    private FlexibleSearchService flexibleSearchService;
 
+   public List<String> findOrderCodesPlacedSince(final Date placedSince) throws SQLException
+   {
+      final List<String> codes = new ArrayList<>();
+      try (final Connection connection = dataSource.getConnection();
+            final PreparedStatement statement = connection.prepareStatement(ORDER_CODES_SINCE))
+      {
+         statement.setDate(1, placedSince);
+         final ResultSet rows = statement.executeQuery();
+         while (rows.next())
+         {
+            codes.add(rows.getString(1));
+         }
+      }
+      return codes;
+   }
 
    public void setDataSource(final DataSource dataSource)
    {
